import React from "react";

export default function Principal({ fixed }) {
    return (
        <>

        </>
    );
}

export default function post({ fixed }) {
    return (
        <>
            <div>
                
            </div>
        </>
    );
}